# ROS package format 3 schema

`package_format3.xsd` is vendored from the canonical ROS schema URL:

`http://download.ros.org/schema/package_format3.xsd`

The local copy keeps `ament_xmllint` deterministic in offline and sandboxed
builds while preserving full package-format validation. Update it only from the
canonical URL and record the new SHA-256 here.

SHA-256: `829bf93f11ae98074068f219abfbe8b739dfb6cb248ea32bfd41ead604989a2d`
